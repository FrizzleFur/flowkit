# prompt 的 Codex 兼容适配层

> 适用环境：OpenAI Codex CLI / DeepSeek dsh。**Claude Code 环境忽略本文件**。

本技能为纯方法论文档（评分框架 + 优化流程），无任何 harness 机制依赖，各平台可直接运行。

唯一注意：调用前缀差异——Claude Code 用 `/prompt`，Codex 用 `$prompt`；dsh 用 `/prompt`。
