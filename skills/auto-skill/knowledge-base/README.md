# knowledge-base（个人数据，本地维护）

存放通用知识条目（可复用流程/偏好/决策逻辑）。**数据文件不入 git**（见 .gitignore）——
公开仓库只保留协议与空骨架，个人经验数据在各设备本地维护与备份。

条目格式见 SKILL.md「knowledge-base 条目格式」。首次使用：直接在此目录创建 `<category>.md` 并向 `_index.json` 的 categories 登记（name/keywords/description/lastUpdated/subject_version）。
