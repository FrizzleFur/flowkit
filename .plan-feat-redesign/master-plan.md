# Master Plan — 三轴综合编排（内容 × 主题色 × 动效）

> 2026-09-13 | 综合 sweep+redesign 全部未竟任务 | 三轴: ①内容（用户判定: 易）②主题色细节对齐 ③动效×章节对位
> 核心原则: 动画奥义优先——每章优势内容选取**语义同构**的动效形态，意义讲得清、不违背常理；适配不照搬

## 一、章节×动效对位表（智力核心，11 章全量）

| 章 | 优势内容（本章之最） | 动效形态 | 奥义（为什么是这个形态） | 现状 | 动作 |
|---|---|---|---|---|---|
| ch1 | 复杂度闸门：任务→flow/deep 分岔 | P12 交互路由台 | 分岔判定空间化——路由不是文字是画面 | ch1-gate ✓ + switch | **已对位** |
| ch2 | 长时程三板斧（三轴对映） | P1 回放（subagent） | 待审计：现回放讲 subagent 还是三板斧？ | ch2-subagent ×1 | **审计①**：错位则补三板斧三段对比 |
| ch3 | 记忆召回闭环 | P13 双面板对比 | 召回前后回答质量=闭环价值 | ch3-recall ✓ | 可选升级: P11 复利曲线叠加（召回质量随沉淀增长） |
| ch4 | 管道全景 | P13 多面板联动 | 全景=一条龙时序 | ch4-pipeline ✓ 大剧本 | **已对位** |
| ch5 | 输入三关（乔哈里/3S/分诊） | P6 判定 + P8 分屏 + P12 双路径 | 输入=认知象限+表达质量+分诊 | johari+feedsplit+paths ✓ | **已对位** |
| ch6 | 评审三层+六原则 | P6+P1 漏斗+P1 双面板 | 三消：沉没成本/盲区/注意力 | 三件 ✓ | **已对位（标杆）** |
| ch7 | 并发预算与分片 | P5 计算器+P9 泳道×4 | 预算可算、分片可见 | budget+4 ✓ | **已对位** |
| ch8 | 上下文容量与恢复 | P1 三部（现状）+ **P7 曲线（缺）** | 上下文 rot=量变滑坡——**天然曲线 aha**，B 库适用判据原文匹配 | 3 replays | **差距②**: C2 三策略对比曲线（curve 面板现成，只写 JSON） |
| ch9 | 验证迭代 keep/revert | P2 轨道步进 | 红旗=闸门、keep/revert=时序 | 2 replays ✓ | 已对位; C13 Fallback 决策树（P12）备选池 |
| ch10 | 跨会话双库复利 | P11 增长 | 复利=雪球 | compound+snowball ✓ | **已对位** |
| ch11 | 三道锁治理 | P6 quiz | 治理=判断题非演示题 | constquiz ✓ | 已对位; C17 封印环备选池 |

**审计结论**: 9/11 已对位。真差距两处——**ch2 错位嫌疑**（回放主题与三板斧的匹配待验）、**ch8 缺曲线形态**（容量滑坡是 B 库判据点名的 curve 适用场景，C2 设计现成）。

## 二、主题色细节对齐专项（轴②）

方法: learncc vs 我方 **computed style 逐项 diff**（bg 阶四档/边框三档/字重/圆角/间距节奏），不再凭肉眼。
已知挂账并入: S8 圆角阶梯（5/7/9/10/14px 未收敛至 4/6/8/12/999）、S9 反馈色 hex 残留 5 处、S12 fs-bd-total 无 tabular、S6 fs-quote-soft 零使用、S16 三处文件名栏。
完成标准: diff 表清零（或显式豁免记录）+ CDP 复验。

## 三、内容收尾（轴①，用户判定易）

- 波 4 推广: ch6 模板（三标签+摘要卡+快用行+双卡）× 7 个机制篇章
- ch12 实战走查章（素材=三周期真实 git 链; 同步 sidebar/path-data/机检 11→12）
- quiz 出题首批（Brown 规则, 3 章 × 2-3 题起步）
- 对位审计产出的新剧本（ch2 补建 + ch8 曲线，均为纯 JSON）

## 四、编排（四阶段，依赖驱动）

| 阶段 | 内容 | 依赖 | 产出 |
|---|---|---|---|
| **S1 对位审计** | ch2/ch3/ch8 逐章深查（现动效 vs 优势内容匹配度）→ 对位表定稿 | 无 | 对位表终版 + 差距清单 |
| **S2 主题细节专项** | learncc computed diff 抽查 → 批修挂账 → CDP diff 复验 | 无（与 S1 并行） | diff 清零 + 抽样截图 |
| **S3 波 4 推广+内容件** | 7 章三标签改造 + S1 产出的新剧本 + ch12 + quiz 首批 | S1 定稿 | 全站对位+结构统一 |
| **S4 终验** | 机检复跑 + CDP 全站抽查（动效/高亮/对位逐章）+ rsync 同步链 | S3 | ALL PASS → 用户 hexo d |

并行性: S1∥S2 可并行（S1 主会话+CDP, S2 纯静态+CDP diff）; S3 依赖 S1; S4 收口。
纪律延续: 零构建/SC5/CDP 强制帧/agent 完成即收/缓存强刷。

## 五、验收（Goal Verification 预告）

- 对位表 11/11 章逐章「动效-内容」匹配有据（意义一句话可述）
- 主题 diff 表清零; 机检 ALL PASS（含 ch12 后 12 章断言）
- CDP 全站抽查: 每章 ≥1 可播放交互 + 高亮 learncc 色板 + 三标签结构
- 同步链: rsync → 用户 hexo d（用户动作）
